---
deployment:
  tasks:
    - export DEPLOYPATH=/home/sink4858/public_html/masjid-screen.singgihcp.com/
    - /bin/cp -R display $DEPLOYPATH
    - /bin/cp -R dist $DEPLOYPATH
    - /bin/cp -R *.php $DEPLOYPATH
